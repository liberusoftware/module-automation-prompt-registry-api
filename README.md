# PromptRegistry API

One-to-one HTTP API adapter for `module-automation-prompt-registry`. Routes are versioned under `/api/v1/automation/prompt-registry`, require authenticated team context, and delegate domain mutations to the core package.

